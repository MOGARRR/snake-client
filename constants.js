const IP = 'localhost';
const PORT = 50541;
const movement = {w: "Move: up", a: "Move: left", s: "Move: down", d: "Move: right"}; // object with movement commnands as values
const messages = {h:'*Hissy fit noises*', j:'hissterical', k: 'boop', i: 'sssssorry'}; // same as movement but instead with messages


module.exports = {
  IP,
  PORT,
  movement,
  messages
};